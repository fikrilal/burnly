# 2026-08-23 Burnly v0.1.29 Release

## Objective

Prepare, validate, publish, and inspect Burnly `v0.1.29`, repairing the
OpenCode 2 assistant-error compatibility failure and the Antigravity legacy
protobuf artifact routing failure introduced in `v0.1.28`.

## Risk Class

`high` — this changes public release metadata, migrates persisted diagnostics,
pushes protected release inputs, creates a version tag, executes signed
multi-platform builds, and promotes a GitHub release consumed by the desktop
updater.

## Release Contract

- `package.json`, `src-tauri/Cargo.toml`, and the Rust lockfile agree on
  `0.1.29`; tag identity is `v0.1.29`.
- Release notes describe both runtime repairs, automatic diagnostics recovery,
  privacy behavior, and the unsigned Windows preview warning.
- Local full, runtime, and release-specific gates pass from a clean release
  commit.
- `development` is pushed and merged through the repository's normal protected
  branch workflow; remote verification succeeds on the release commit.
- The tag points at the verified mainline release commit.
- All platform jobs, updater signatures, checksum manifests, provenance, and
  draft publication succeed.
- The draft notes and asset matrix are inspected before public promotion.

## Checklist

- [x] Commit the completed collector runtime hotfix.
- [x] Bump release metadata to `0.1.29`.
- [x] Add reviewed `v0.1.29` release notes.
- [x] Run release-specific gates, `pnpm verify`, and `pnpm verify:runtime`.
- [ ] Commit release preparation from a clean tree.
- [ ] Push `development`, merge through the normal branch workflow, and confirm
      remote verification.
- [ ] Create and push annotated tag `v0.1.29` on the verified mainline commit.
- [ ] Confirm validation and every platform release job succeeds.
- [ ] Inspect draft notes, asset names, sizes, signatures, checksums, updater
      metadata, and attestations.
- [ ] Promote the inspected draft release publicly.
- [ ] Record release URL, workflow evidence, final commit/tag identity, and
      archive this plan.

## Rollback And Stop Conditions

- Do not move or recreate an already-pushed tag. Fix forward with a new patch
  version if published inputs are wrong.
- Do not publish a partial matrix or a release missing updater signatures,
  checksums, installer helpers, or either updater manifest.
- If verification or a platform build fails, leave the release as a draft or
  absent, diagnose, and require the corrected commit to pass before promotion.
- Publishing is irreversible from an updater-consumer perspective; release
  deletion is not treated as rollback.

## Verification

- `pnpm release:version v0.1.29`: passed; package and Cargo versions match the
  requested tag.
- `pnpm release-workflow:check`, `pnpm packaging:check`,
  `pnpm release-artifacts:test`, and `pnpm updater-metadata:test`: passed.
- `pnpm verify`: passed with 98 frontend tests and 661 Rust tests; three opt-in
  runtime tests were ignored. Formatting, linting, types, strict Clippy, and all
  architecture/security/packaging/release/platform/API/contract/migration/
  sidecar/fixture/pricing harnesses passed. The duplication report remained
  informational.
- `pnpm verify:runtime`: passed on Ubuntu 24.04 x64 GNOME/X11 with eight Tauri
  IPC bridge, twelve lifecycle/tray, and three scheduler tests. Existing
  dependency-update and Vite bundle-size notices were non-blocking.

## Release Evidence

- Collector runtime hotfix: `d81d061`.
