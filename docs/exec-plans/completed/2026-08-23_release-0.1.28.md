# 2026-08-23 Burnly v0.1.28 Release

## Objective

Prepare, validate, publish, and inspect Burnly `v0.1.28`, containing the unified
native OpenCode/OpenCode 2 collector and the Antigravity timestamp-less CLI
usage recovery fix.

## Risk Class

`high` — this changes public release metadata, pushes protected release inputs,
creates a version tag, executes signed multi-platform builds, and promotes a
GitHub release consumed by the desktop updater.

## Release Contract

- `package.json`, `src-tauri/Cargo.toml`, and the Rust lockfile agree on
  `0.1.28`; tag identity is `v0.1.28`.
- Release notes describe unified OpenCode V1/V2 behavior, preview V2 schema
  compatibility, the Antigravity timestamp fix, privacy/recovery behavior, and
  the unsigned Windows preview warning.
- Local full, runtime, and release-specific gates pass from a clean release
  commit.
- `development` is pushed and merged through the repository's normal protected
  branch workflow; remote verification succeeds on the release commit.
- The tag points at the verified mainline release commit.
- All five platform jobs, updater signatures, checksum manifests, provenance,
  and draft publication succeed.
- The draft notes and asset matrix are inspected before public promotion.

## Checklist

- [x] Commit completed OpenCode chunk 6.
- [x] Bump release metadata to `0.1.28`.
- [x] Add reviewed `v0.1.28` release notes.
- [x] Run `pnpm release:version v0.1.28`, release harnesses, `pnpm verify`, and
      `pnpm verify:runtime`.
- [x] Commit release preparation from a clean tree.
- [x] Push `development`, merge through the normal branch workflow, and confirm
      remote verification.
- [x] Create and push annotated tag `v0.1.28` on the verified mainline commit.
- [x] Confirm validation and all five platform release jobs succeed.
- [x] Inspect draft notes, asset names, sizes, signatures, checksums, updater
      metadata, and attestations.
- [x] Promote the inspected draft release publicly.
- [x] Record release URL, workflow evidence, final commit/tag identity, and
      archive this plan.

## Rollback And Stop Conditions

- Do not move or recreate an already-pushed tag. Fix forward with a new patch
  version if published inputs are wrong.
- Do not publish a partial matrix or a release missing updater signatures,
  checksums, installer helpers, or either updater manifest.
- If verification or a platform build fails, leave the release as a draft or
  absent, diagnose, and require the corrected commit to pass before promotion.
- Publishing is irreversible from an updater-consumer perspective; release
  deletion is not treated as rollback.

## Verification

- `pnpm release:version v0.1.28`: passed; package and Cargo versions match the
  requested tag.
- `pnpm release-workflow:check`, `pnpm packaging:check`,
  `pnpm release-artifacts:test`, and `pnpm updater-metadata:test`: passed.
- `pnpm verify`: passed with 98 frontend tests and 655 Rust tests; 2 opt-in
  runtime tests were ignored. Formatting, linting, types, strict Clippy, and all
  architecture/security/packaging/release/platform/API/contract/migration/
  sidecar/fixture/pricing harnesses passed. The duplication report remained
  informational.
- `pnpm verify:runtime`: passed on Ubuntu 24.04 x64 GNOME/X11 with 8 Tauri IPC
  bridge, 12 lifecycle/tray, and 3 scheduler tests. Existing dependency-update
  and Vite bundle-size notices were non-blocking.

## Release Evidence

- OpenCode validation chunk: `93a30e3`.
- Release preparation commit: `35d1a59 chore(release): v0.1.28`.
- Pull request: [#14](https://github.com/fikrilal/burnly/pull/14), merged through
  the normal merge-commit workflow as `d98040af73036f31efa682ccddf98e890594e785`.
- Remote verification passed for the
  [development push](https://github.com/fikrilal/burnly/actions/runs/32615600840),
  [pull request](https://github.com/fikrilal/burnly/actions/runs/32615896459),
  and [main merge](https://github.com/fikrilal/burnly/actions/runs/32616233048).
  The only annotation was GitHub's non-blocking Node 20 action-runtime
  deprecation notice for `pnpm/action-setup`.
- Annotated tag `v0.1.28` resolves to verified main commit
  `d98040af73036f31efa682ccddf98e890594e785`.
- [Release workflow](https://github.com/fikrilal/burnly/actions/runs/32616551577)
  passed validation, Linux x64/arm64, Windows x64, macOS Intel/Apple Silicon,
  provenance attestation, updater metadata generation, and draft publication.
- The downloaded draft contained 25 non-empty assets with GitHub SHA-256
  digests. `SHA256SUMS` validated all 19 listed files, both updater manifests
  passed `pnpm updater:verify`, the workflow-equivalent canonical subset passed
  `pnpm release:verify` for seven distributable artifacts, and all seven
  distributables passed `gh attestation verify`.
- The inspected draft was promoted on 2026-08-23 at 04:09:39 UTC. Public
  release: [Burnly v0.1.28](https://github.com/fikrilal/burnly/releases/tag/v0.1.28).
  GitHub reports it as the latest non-prerelease release, and the latest updater
  URL resolves successfully.
