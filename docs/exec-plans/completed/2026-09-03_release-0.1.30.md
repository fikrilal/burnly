# 2026-09-03 Burnly v0.1.30 Release

## Objective

Prepare, validate, publish, and inspect Burnly `v0.1.30`, introducing
undated historical baseline attribution for Antigravity conversation records
without source timestamps, automated profile-2 historical repair, account-safe
cloud-sync tombstones, and resilient refresh orchestration.

## Risk Class

`high` — this changes public release metadata, migrates persisted diagnostics,
pushes protected release inputs, creates a version tag, executes signed
multi-platform builds, and promotes a GitHub release consumed by the desktop
updater.

## Release Contract

- `package.json`, `src-tauri/Cargo.toml`, and the Rust lockfile agree on
  `0.1.30`; tag identity is `v0.1.30`.
- Release notes describe undated baseline attribution, automated profile-2
  historical repair, account-safe collect-sync tombstones, privacy behavior, and
  the unsigned Windows preview warning.
- Local full, runtime, and release-specific gates pass from a clean release
  commit.
- `development` is pushed and merged through the repository's normal protected
  branch workflow; remote verification succeeds on the release commit.
- The tag points at the verified mainline release commit.
- All platform jobs, updater signatures, checksum manifests, provenance, and
  draft publication succeed.
- The draft notes and asset matrix are inspected before public promotion.

## Checklist

- [x] Bump release metadata to `0.1.30`.
- [x] Add reviewed `v0.1.30` release notes.
- [x] Run release-specific gates, `pnpm verify`, and `pnpm verify:runtime`.
- [x] Commit release preparation from a clean tree.
- [x] Push `development`, merge through the normal branch workflow, and confirm
      remote verification.
- [x] Create and push annotated tag `v0.1.30` on the verified mainline commit.
- [x] Confirm validation and every platform release job succeeds.
- [x] Inspect draft notes, asset names, sizes, signatures, checksums, updater
      metadata, and attestations.
- [x] Promote the inspected draft release publicly.
- [x] Record release URL, workflow evidence, final commit/tag identity, and
      archive this plan.

## Rollback And Stop Conditions

- Do not move or recreate an already-pushed tag. Fix forward with a new patch
  version if published inputs are wrong.
- Do not publish a partial matrix or a release missing updater signatures,
  checksums, installer helpers, or either updater manifest.
- If verification or a platform build fails, leave the release as a draft or
  absent, diagnose, and require the corrected commit to pass before promotion.
- Publishing is irreversible from an updater-consumer perspective; release
  deletion is not treated as rollback.

## Verification

- `pnpm release:version v0.1.30`: passed; `package.json` and `Cargo.toml` agree on `0.1.30`.
- `pnpm release-workflow:check`, `pnpm packaging:check`,
  `pnpm release-artifacts:test`, and `pnpm updater-metadata:test`: all passed.
- `pnpm architecture:check`: passed (0 boundary violations).
- `pnpm verify:fast`: passed (Prettier, ESLint, TypeScript, unit tests, JSCPD).
- `pnpm verify`: passed with 117 frontend tests and 709 Rust unit/integration tests.
- `pnpm verify:runtime`: passed with Vite production build (2438 modules), desktop evidence,
  8 Tauri IPC bridge tests, 12 lifecycle/tray unit tests, and 3 scheduler tests.

## Release Evidence

- Release preparation commit: `4fe6732 chore(release): v0.1.30`.
- Pull request: [#16](https://github.com/fikrilal/burnly/pull/16), merged as
  `2e47adb6883ba56f4d36eb53e79a81da1615f336`.
- Remote verification passed:
  - [development push](https://github.com/fikrilal/burnly/actions/runs/33767659094)
  - [pull request](https://github.com/fikrilal/burnly/actions/runs/33767703297)
  - [main push](https://github.com/fikrilal/burnly/actions/runs/33768444583)
- Annotated tag `v0.1.30` resolves to verified main commit `2e47adb6883ba56f4d36eb53e79a81da1615f336`.
- [Release workflow](https://github.com/fikrilal/burnly/actions/runs/33768514754)
  passed validation, all 5 platform builds (Linux x64, Linux ARM64, Windows x64, macOS Intel,
  macOS Apple Silicon), provenance attestation, updater metadata generation, and draft publication.
- Release contains 25 non-empty assets with SHA-256 digests in `SHA256SUMS`, signed updater
  artifacts, and cross-platform `latest.json` / `latest-linux.json` manifests.
- Public release: [Burnly v0.1.30](https://github.com/fikrilal/burnly/releases/tag/v0.1.30).
